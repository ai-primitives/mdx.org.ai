{
  "name": "@mdx.org.ai/epcis",
  "version": "0.1.0",
  "description": "Modern EPCIS Repository using Cloudflare Workers, Pipelines, and Analytics Engine",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc && wrangler types",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "clean": "rm -rf dist src/**/*.d.ts"
  },
  "dependencies": {
    "@clickhouse/client": "^0.2.7",
    "@types/uuid": "^10.0.0",
    "hono": "^3.12.0",
    "uuid": "^11.0.3",
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.2",
    "wrangler": "^3.22.1"
  }
}
